# Session note — 2026-08-19

**Track:** main (learn) | **Phase:** 0
**Time:** ~90 min

## What I did
- Kaggle *Pandas* lesson 5 (dtypes and missing data) and lesson 6 (renaming and combining), both exercise notebooks passed.
- Worked out why a column shows `object` when you expected numbers: either it is text doing its job, or it is a numeric column held hostage by a few string sentinels ("Unknown", "Undisclosed"). `dtypes` cannot tell them apart; `value_counts()` can — text gives ~130k distinct values each seen once, the hostage case gives numbers plus one string entry with a count.
- Transfer question on combining: two pulls of the same city a week apart, stacked with `concat`, produce duplicate rows because a forward calendar re-lists every still-upcoming show. Two pulls of *different* cities do not — the question to ask of any concat is what makes a row unique.
- Follow-on asked unprompted: `drop_duplicates(subset=[...])`, shown on toy data only. `subset` is the columns that define "same row"; with no subset a row must match on every column, which fails when a re-pull changes a field.

## What I made (link or path)
- Kaggle Pandas certificate, on her Kaggle profile. The course is finished — 6 lessons, 6 exercises.

## What broke and how it got fixed
- Nothing broke. One transfer question was too vague as first asked and got sharpened after she said so.

## One thing I'd forget by next week
- The functions and syntax from lesson 6 — specifically how `join` and `concat` behave differently.

**Next action:** Skill session 3 — agentic course, lab 2 outstanding from skill session 1.
