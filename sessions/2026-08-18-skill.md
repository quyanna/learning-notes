# Session note — 2026-08-18

**Track:** main (learn) | **Phase:** 0
**Time:** ~90 min

## What I did
- Week 1 **Day 2** of the agentic course — videos 9, 10 and 11, the theory day
  with no lab: what an AI agent is, Anthropic's design patterns, and the risks /
  guardrails / traps lecture.
- Settled the definition: an agent is **an LLM with tools in a loop to achieve a
  goal**, and the agent is *my code* around the model, not the model.
- Placed my own lab 1 exercise (three chained calls) as **prompt chaining**, a
  workflow.
- Answered the question video 10 parked — when to split one LLM call into
  several — from video 11: start with one call, one prompt, one objective;
  measure against a business metric; split only when splitting measurably
  improves it.
- Cleared up three things that get conflated: a **business eval** (a number
  outside the system), a **ground-truth eval** (my own answer key — what
  project 3's golden questions are), and **LLM as a judge** (a second model
  grades the output, used when there is no answer key).

## What I made (link or path)
- `til/agentic-concepts.md` — new running file. Agent vs workflow, the five
  workflow patterns + the agent pattern, risks and unpredictability,
  observability and evals, guardrails, both traps, hallucination, and the
  "agentic engineer" ambiguity.
- `resources/agentic-udemy.md` — added the week 1 video↔lab map (which Day's
  videos cover which lab, with running times).
- Shelf entry `anthropic-building-effective-agents`, verified.

## What broke and how it got fixed
- Day 2 had never been assigned. The resource file indexes rows by *lab*, and
  Day 2 has no lab, so its three videos had no row and fell through. Fixed by
  writing the full week 1 video map into the resource file.
- Lab 2 was still outstanding from session 1 — the setup fight took that
  session. It moves to skill session 3 rather than getting squeezed in here.
- Week 1 is ~5 hours of video against three sprint skill sessions (~4.5 hours
  total, labs included). It does not fit. Logged, not silently cut.

## One thing I'd forget by next week
- Parallelization and orchestrator-worker look like the same diagram. The
  difference is **who decides the split**: my code (parallelization) or an LLM
  (orchestrator-worker). And orchestrator-worker is still a *workflow* — the
  code path is predefined even though a model picks what happens inside it.

**Next action:** Settle the Python/SQL on-ramp decision, then skill session 3 — Day 3 videos 12–16 and lab 2 (OpenAI cells + Ollama; skip the providers with no key).
