# Session note — 2026-08-17

**Track:** main (learn) | **Phase:** 0
**Time:** ~90 min

## What I did
- Set up the Ed Donner agentic course environment from scratch: cloned
  `ed-donner/agents`, ran `uv sync` clean (`.venv`, Python 3.12.12), loaded $5
  of OpenAI credit with auto-recharge disabled, created `.env` with
  `OPENAI_API_KEY` (confirmed loading, confirmed gitignored at `.gitignore:131`).
- Ran `1_foundations/1_lab1.ipynb` end to end.
- Wrote the lab 1 exercise myself: three chained calls where each model answer
  becomes the next prompt — business area, then a pain point in that area, then
  an agentic solution to that pain point.

## What I made (link or path)
- `~/code/agents` — working environment, lab 1 run, exercise written.

## What broke and how it got fixed
- The system pointed me at `setup/SETUP-PC.md`; I work in WSL2, so
  `setup/SETUP-linux.md` was the right guide. Resource file and shelf updated.
- The exercise cell looked hung. It wasn't — three sequential calls to
  `gpt-5.4` (the largest model) in one cell, each waiting for a full response.
  A busy kernel shows `[*]` exactly like a hang. Fix is to split it into three
  cells and use `nano`/`mini` for the cheap steps, as the lab's own cells do.
- OpenAI's docs now steer at the Responses API; Chat Completions has its own
  reference page. The course uses `chat.completions` because every other
  provider copied that shape — which is why lab 2 can point one `OpenAI()`
  client at Anthropic, DeepSeek, Gemini, Groq and Ollama by swapping `base_url`.
  Responses is OpenAI-only.

## One thing I'd forget by next week
- Every cell in lab 1 is the same three lines: build `messages`, call
  `.create(...)`, dig the text out of `response.choices[0].message.content`.
  Everything else is a different string in the middle.

**Next action:** Skill session 2 — week 1 labs 3–4, with the Udemy videos open, starting with 20 min on how a `chat.completions` call is actually structured.
