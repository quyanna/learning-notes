# Session note — 2026-08-19

**Track:** main (build) | **Phase:** 0
**Time:** ~90 min

## What I did
- Settled the research question, in my own words: of artists scheduled to play Vancouver, what share also play Calgary? And of those who don't, what share play Edmonton instead?
- Chose four cities for the pull — Vancouver, Calgary, Edmonton, Toronto. Edmonton is the control (is it Calgary, or all of Alberta?); Toronto separates a national tour from a western swing.
- Wrote `docs/QUESTION.md`, a full research design: attraction-level genre tags, `attraction.id` as the join key rather than names, filtering decisions, known limits.
- Learned the API request sequence on unrelated toy data (USGS earthquakes) before touching Ticketmaster: `params` as a dict, `timeout`, `raise_for_status()`, `.json()`.
- Learned the envelope idea — an API returns a wrapper with the items under one key (`_embedded.events`) and a count under another (`page.totalElements`).
- Credentials: `.env` added to `.gitignore`, `.env.example` committed, `python-dotenv` installed, key read with `os.environ`.
- Wrote and ran `extract.py`. 200 Vancouver music events on disk as JSONL.

## What I made (link or path)
- `extract.py` — Vancouver, music, 2026-09-01 to 2026-11-30, one page.
- `data/raw/events_20260819.jsonl` — 200 real events, 2.1 MB.
- `docs/QUESTION.md`.

## What broke and how it got fixed
- `.env` was not in `.gitignore` — added before the key was ever written to disk.
- Thought `extract.py` had produced nothing. It had; the file was on disk and the editor tree hadn't refreshed.

## One thing I'd forget by next week
- The cities are the only thing that changes `extract.py`. Genre cuts, US-vs-Canadian artist cuts, which city is the anchor — all of those are notebook decisions made later on data already pulled. Don't re-pull to change the analysis.

**Next action:** interview session — one Blind 75 problem in Python, then one SQL 50 problem.
