# Session note — 2026-08-18

**Track:** main (build) | **Phase:** 0
**Time:** ~90 min

## What I did
- Chose the dataset: Ticketmaster Discovery API (free key, 5000 calls/day, 5 req/sec, deep-paging ceiling of size × page < 1000).
- Worked a vague curiosity into a question with a denominator, through three phrasings.
- Learned what `uv add` does: writes pyproject.toml, pins uv.lock, creates .venv, installs. `uv run` is what makes the venv's Python the one that runs.
- Learned what unit of analysis means — one row = one artist, not one event — and why counting by event would have reported 33% where counting by artist reports 50%.
- Created the repo, committed, pushed public with `gh repo create --source=. --push`.

## What I made (link or path)
- https://github.com/quyanna/calgary-tour-routing — public, README carries the question.
- ~/code/calgary-tour-routing with pandas, requests, pyarrow installed.

## What broke and how it got fixed
- `gh repo create --push` would have failed with zero commits: `uv init` runs `git init` but commits nothing. Fixed by `git add -A && git commit` first.
- Typed `git auth login` instead of `gh auth login`.

## One thing I'd forget by next week
- `uv run python ...` is not the same as `python ...` — only `uv run` uses the project's `.venv`, so a plain `python` call finds no pandas.

**Next action:** interview session — one Blind 75 problem in Python, then one SQL 50 problem.
